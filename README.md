# cogniform
A deterministic, headless-first 3D scene materialization engine in Rust that turns agent intent into revisioned worlds, renders machine-readable observations, and closes the agent–world feedback loop.
