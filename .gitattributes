* text=auto eol=lf

*.bat text eol=crlf
*.cmd text eol=crlf
*.ps1 text eol=crlf

# Cargo verifies vendored package files byte-for-byte against these sources.
# Registry archives may retain CRLF, which is part of those verified bytes.
/vendor/** -text whitespace=cr-at-eol,-blank-at-eof
